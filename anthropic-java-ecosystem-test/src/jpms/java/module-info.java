// Requires the umbrella module alone, so everything `JpmsUsageMain` touches must arrive through the
// SDK's own `requires transitive` directives.
module com.anthropic.ecosystem.jpms {
    requires com.anthropic;
}
