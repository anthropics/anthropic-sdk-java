// Code generated by Wire protocol buffer compiler, do not edit.
// Source: kotlinx.kmp.util.ICalFreeBusy in types.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package kotlinx.kmp.util

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Instant
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import kotlin.collections.List
import okio.ByteString

/**
 * RFC 5545 §3.6.4 VFREEBUSY — free/busy time info.
 */
public class ICalFreeBusy(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 0,
  )
  public val uid: String = "",
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#INSTANT",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "dtStart",
    schemaIndex = 1,
  )
  public val dt_start: Instant? = null,
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#INSTANT",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "dtEnd",
    schemaIndex = 2,
  )
  public val dt_end: Instant? = null,
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 3,
  )
  public val organizer: String = "",
  attendees: List<ICalAttendee> = emptyList(),
  free_busy: List<ICalFreeBusyPeriod> = emptyList(),
  @field:WireField(
    tag = 7,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 6,
  )
  public val url: String = "",
  @field:WireField(
    tag = 8,
    adapter = "com.squareup.wire.ProtoAdapter#INSTANT",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 7,
  )
  public val dtstamp: Instant? = null,
  @field:WireField(
    tag = 9,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 8,
  )
  public val comment: String = "",
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<ICalFreeBusy, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 5,
    adapter = "kotlinx.kmp.util.ICalAttendee#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 4,
  )
  public val attendees: List<ICalAttendee> = immutableCopyOf("attendees", attendees)

  @field:WireField(
    tag = 6,
    adapter = "kotlinx.kmp.util.ICalFreeBusyPeriod#ADAPTER",
    label = WireField.Label.REPEATED,
    jsonName = "freeBusy",
    schemaIndex = 5,
  )
  public val free_busy: List<ICalFreeBusyPeriod> = immutableCopyOf("free_busy", free_busy)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is ICalFreeBusy) return false
    if (unknownFields != other.unknownFields) return false
    if (uid != other.uid) return false
    if (dt_start != other.dt_start) return false
    if (dt_end != other.dt_end) return false
    if (organizer != other.organizer) return false
    if (attendees != other.attendees) return false
    if (free_busy != other.free_busy) return false
    if (url != other.url) return false
    if (dtstamp != other.dtstamp) return false
    if (comment != other.comment) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + uid.hashCode()
      result = result * 37 + (dt_start?.hashCode() ?: 0)
      result = result * 37 + (dt_end?.hashCode() ?: 0)
      result = result * 37 + organizer.hashCode()
      result = result * 37 + attendees.hashCode()
      result = result * 37 + free_busy.hashCode()
      result = result * 37 + url.hashCode()
      result = result * 37 + (dtstamp?.hashCode() ?: 0)
      result = result * 37 + comment.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """uid=${sanitize(uid)}"""
    if (dt_start != null) result += """dt_start=$dt_start"""
    if (dt_end != null) result += """dt_end=$dt_end"""
    result += """organizer=${sanitize(organizer)}"""
    if (attendees.isNotEmpty()) result += """attendees=$attendees"""
    if (free_busy.isNotEmpty()) result += """free_busy=$free_busy"""
    result += """url=${sanitize(url)}"""
    if (dtstamp != null) result += """dtstamp=$dtstamp"""
    result += """comment=${sanitize(comment)}"""
    return result.joinToString(prefix = "ICalFreeBusy{", separator = ", ", postfix = "}")
  }

  public fun copy(
    uid: String = this.uid,
    dt_start: Instant? = this.dt_start,
    dt_end: Instant? = this.dt_end,
    organizer: String = this.organizer,
    attendees: List<ICalAttendee> = this.attendees,
    free_busy: List<ICalFreeBusyPeriod> = this.free_busy,
    url: String = this.url,
    dtstamp: Instant? = this.dtstamp,
    comment: String = this.comment,
    unknownFields: ByteString = this.unknownFields,
  ): ICalFreeBusy = ICalFreeBusy(uid, dt_start, dt_end, organizer, attendees, free_busy, url, dtstamp, comment, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<ICalFreeBusy> = object : ProtoAdapter<ICalFreeBusy>(
      FieldEncoding.LENGTH_DELIMITED, 
      ICalFreeBusy::class, 
      "type.googleapis.com/kotlinx.kmp.util.ICalFreeBusy", 
      PROTO_3, 
      null, 
      "types.proto"
    ) {
      override fun encodedSize(`value`: ICalFreeBusy): Int {
        var size = value.unknownFields.size
        if (value.uid != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.uid)
        }
        if (value.dt_start != null) {
          size += ProtoAdapter.INSTANT.encodedSizeWithTag(2, value.dt_start)
        }
        if (value.dt_end != null) {
          size += ProtoAdapter.INSTANT.encodedSizeWithTag(3, value.dt_end)
        }
        if (value.organizer != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(4, value.organizer)
        }
        size += ICalAttendee.ADAPTER.asRepeated().encodedSizeWithTag(5, value.attendees)
        size += ICalFreeBusyPeriod.ADAPTER.asRepeated().encodedSizeWithTag(6, value.free_busy)
        if (value.url != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(7, value.url)
        }
        if (value.dtstamp != null) {
          size += ProtoAdapter.INSTANT.encodedSizeWithTag(8, value.dtstamp)
        }
        if (value.comment != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(9, value.comment)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: ICalFreeBusy) {
        if (value.uid != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.uid)
        }
        if (value.dt_start != null) {
          ProtoAdapter.INSTANT.encodeWithTag(writer, 2, value.dt_start)
        }
        if (value.dt_end != null) {
          ProtoAdapter.INSTANT.encodeWithTag(writer, 3, value.dt_end)
        }
        if (value.organizer != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 4, value.organizer)
        }
        ICalAttendee.ADAPTER.asRepeated().encodeWithTag(writer, 5, value.attendees)
        ICalFreeBusyPeriod.ADAPTER.asRepeated().encodeWithTag(writer, 6, value.free_busy)
        if (value.url != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 7, value.url)
        }
        if (value.dtstamp != null) {
          ProtoAdapter.INSTANT.encodeWithTag(writer, 8, value.dtstamp)
        }
        if (value.comment != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 9, value.comment)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: ICalFreeBusy) {
        writer.writeBytes(value.unknownFields)
        if (value.comment != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 9, value.comment)
        }
        if (value.dtstamp != null) {
          ProtoAdapter.INSTANT.encodeWithTag(writer, 8, value.dtstamp)
        }
        if (value.url != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 7, value.url)
        }
        ICalFreeBusyPeriod.ADAPTER.asRepeated().encodeWithTag(writer, 6, value.free_busy)
        ICalAttendee.ADAPTER.asRepeated().encodeWithTag(writer, 5, value.attendees)
        if (value.organizer != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 4, value.organizer)
        }
        if (value.dt_end != null) {
          ProtoAdapter.INSTANT.encodeWithTag(writer, 3, value.dt_end)
        }
        if (value.dt_start != null) {
          ProtoAdapter.INSTANT.encodeWithTag(writer, 2, value.dt_start)
        }
        if (value.uid != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.uid)
        }
      }

      override fun decode(reader: ProtoReader): ICalFreeBusy {
        var uid: String = ""
        var dt_start: Instant? = null
        var dt_end: Instant? = null
        var organizer: String = ""
        val attendees = mutableListOf<ICalAttendee>()
        val free_busy = mutableListOf<ICalFreeBusyPeriod>()
        var url: String = ""
        var dtstamp: Instant? = null
        var comment: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> uid = ProtoAdapter.STRING.decode(reader)
            2 -> dt_start = ProtoAdapter.INSTANT.decode(reader)
            3 -> dt_end = ProtoAdapter.INSTANT.decode(reader)
            4 -> organizer = ProtoAdapter.STRING.decode(reader)
            5 -> attendees.add(ICalAttendee.ADAPTER.decode(reader))
            6 -> free_busy.add(ICalFreeBusyPeriod.ADAPTER.decode(reader))
            7 -> url = ProtoAdapter.STRING.decode(reader)
            8 -> dtstamp = ProtoAdapter.INSTANT.decode(reader)
            9 -> comment = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return ICalFreeBusy(
          uid = uid,
          dt_start = dt_start,
          dt_end = dt_end,
          organizer = organizer,
          attendees = attendees,
          free_busy = free_busy,
          url = url,
          dtstamp = dtstamp,
          comment = comment,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: ICalFreeBusy): ICalFreeBusy = value.copy(
        dt_start = value.dt_start?.let(ProtoAdapter.INSTANT::redact),
        dt_end = value.dt_end?.let(ProtoAdapter.INSTANT::redact),
        attendees = value.attendees.redactElements(ICalAttendee.ADAPTER),
        free_busy = value.free_busy.redactElements(ICalFreeBusyPeriod.ADAPTER),
        dtstamp = value.dtstamp?.let(ProtoAdapter.INSTANT::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
