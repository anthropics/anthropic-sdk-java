// Code generated by Wire protocol buffer compiler, do not edit.
// Source: io_petstore.Pet in petstore.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package io_petstore

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import kotlin.collections.List
import okio.ByteString

public class Pet(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 0,
  )
  public val id: Long = 0L,
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 1,
  )
  public val name: String = "",
  @field:WireField(
    tag = 3,
    adapter = "io_petstore.Category#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 2,
  )
  public val category: Category? = null,
  photoUrls: List<String> = emptyList(),
  tags: List<Tag> = emptyList(),
  @field:WireField(
    tag = 6,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 5,
  )
  public val status: String = "",
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<Pet, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.REPEATED,
    schemaIndex = 3,
  )
  public val photoUrls: List<String> = immutableCopyOf("photoUrls", photoUrls)

  @field:WireField(
    tag = 5,
    adapter = "io_petstore.Tag#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 4,
  )
  public val tags: List<Tag> = immutableCopyOf("tags", tags)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is Pet) return false
    if (unknownFields != other.unknownFields) return false
    if (id != other.id) return false
    if (name != other.name) return false
    if (category != other.category) return false
    if (photoUrls != other.photoUrls) return false
    if (tags != other.tags) return false
    if (status != other.status) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + id.hashCode()
      result = result * 37 + name.hashCode()
      result = result * 37 + (category?.hashCode() ?: 0)
      result = result * 37 + photoUrls.hashCode()
      result = result * 37 + tags.hashCode()
      result = result * 37 + status.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """id=$id"""
    result += """name=${sanitize(name)}"""
    if (category != null) result += """category=$category"""
    if (photoUrls.isNotEmpty()) result += """photoUrls=${sanitize(photoUrls)}"""
    if (tags.isNotEmpty()) result += """tags=$tags"""
    result += """status=${sanitize(status)}"""
    return result.joinToString(prefix = "Pet{", separator = ", ", postfix = "}")
  }

  public fun copy(
    id: Long = this.id,
    name: String = this.name,
    category: Category? = this.category,
    photoUrls: List<String> = this.photoUrls,
    tags: List<Tag> = this.tags,
    status: String = this.status,
    unknownFields: ByteString = this.unknownFields,
  ): Pet = Pet(id, name, category, photoUrls, tags, status, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<Pet> = object : ProtoAdapter<Pet>(
      FieldEncoding.LENGTH_DELIMITED, 
      Pet::class, 
      "type.googleapis.com/io_petstore.Pet", 
      PROTO_3, 
      null, 
      "petstore.proto"
    ) {
      override fun encodedSize(`value`: Pet): Int {
        var size = value.unknownFields.size
        if (value.id != 0L) {
          size += ProtoAdapter.INT64.encodedSizeWithTag(1, value.id)
        }
        if (value.name != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(2, value.name)
        }
        if (value.category != null) {
          size += Category.ADAPTER.encodedSizeWithTag(3, value.category)
        }
        size += ProtoAdapter.STRING.asRepeated().encodedSizeWithTag(4, value.photoUrls)
        size += Tag.ADAPTER.asRepeated().encodedSizeWithTag(5, value.tags)
        if (value.status != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(6, value.status)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: Pet) {
        if (value.id != 0L) {
          ProtoAdapter.INT64.encodeWithTag(writer, 1, value.id)
        }
        if (value.name != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.name)
        }
        if (value.category != null) {
          Category.ADAPTER.encodeWithTag(writer, 3, value.category)
        }
        ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 4, value.photoUrls)
        Tag.ADAPTER.asRepeated().encodeWithTag(writer, 5, value.tags)
        if (value.status != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 6, value.status)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: Pet) {
        writer.writeBytes(value.unknownFields)
        if (value.status != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 6, value.status)
        }
        Tag.ADAPTER.asRepeated().encodeWithTag(writer, 5, value.tags)
        ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 4, value.photoUrls)
        if (value.category != null) {
          Category.ADAPTER.encodeWithTag(writer, 3, value.category)
        }
        if (value.name != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.name)
        }
        if (value.id != 0L) {
          ProtoAdapter.INT64.encodeWithTag(writer, 1, value.id)
        }
      }

      override fun decode(reader: ProtoReader): Pet {
        var id: Long = 0L
        var name: String = ""
        var category: Category? = null
        val photoUrls = mutableListOf<String>()
        val tags = mutableListOf<Tag>()
        var status: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> id = ProtoAdapter.INT64.decode(reader)
            2 -> name = ProtoAdapter.STRING.decode(reader)
            3 -> category = Category.ADAPTER.decode(reader)
            4 -> photoUrls.add(ProtoAdapter.STRING.decode(reader))
            5 -> tags.add(Tag.ADAPTER.decode(reader))
            6 -> status = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return Pet(
          id = id,
          name = name,
          category = category,
          photoUrls = photoUrls,
          tags = tags,
          status = status,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: Pet): Pet = value.copy(
        category = value.category?.let(Category.ADAPTER::redact),
        tags = value.tags.redactElements(Tag.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
